import 'package:flutter/material.dart';

class PersonalizedJobs extends StatelessWidget {
  const PersonalizedJobs({ Key? key }) : super(key: key);
  static const routeName= '/personalized-jobs';
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}